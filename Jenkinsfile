pipeline {
  agent {
    node {
      label 'master'
    }
  }
  stages {
        stage('terrafrom started') {
            steps {
                sh 'echo "Started...!" '
            }
        }
    }
}
